import React from 'react';
import CircularProgress from '@material-ui/core/CircularProgress';
const LoadIcon = ({name}) => (
    <CircularProgress style={{ 'margin': '45%' }} />
);

export default LoadIcon;
